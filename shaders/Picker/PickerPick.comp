#version 460

#extension GL_GOOGLE_include_directive : require

#include "PickerPick.h.glsl"

layout(local_size_x = 1) in;

void main()
{
	uvec4 read = imageLoad(pickerImage, pickerBuffer.d.uv);
	pickerBuffer.d.read.x = read.x;
	pickerBuffer.d.read.y = read.y;
}